import image from '../../../../assets/images/profile_photo_no_bg.png';

export const Header = () => {
  return (
    <div>
      <img width={400} src={image} />
    </div>
  );
};

export default Header;
